package com.sunnyday.lychat;

/**
 * LyChat模块应用类
 * 注意：此应用类不应该被单独启动，应该通过主应用 RuoYiApplication 启动
 * 此类已被禁用，避免与主应用冲突
 * 如果需要单独启动，请移除 @SpringBootApplication 注解或使用不同的包扫描路径
 */
// @SpringBootApplication(scanBasePackages = {})
public class LyChatApplication {

    // 此方法不应该被调用，实际启动应该使用 RuoYiApplication
    // public static void main(String[] args) {
    //     SpringApplication.run(LyChatApplication.class, args);
    // }

}
